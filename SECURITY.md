# Security

本包刻意不包含任何账号权限或历史业务数据。请勿把下列内容提交、压缩或上传到共享仓库：`.env*`、Cookie、OAuth token、API key、私钥、浏览器 Profile、真实达人名单、邮件、附件、报价、备份、运行日志。

将真实配置保存在本机私有目录；只复制 `templates/.env.example` 为本机 `.env` 并填写自己的值。发现疑似凭据泄露时，立即停止共享、撤销/轮换对应凭据，并重新构建发布包。
