[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --import-mode=importlib
markers =
    external: requires network access or an external account and is excluded from release smoke tests
