# Validation Scenarios

- Instagram `content-first` hashtag/topic discovery aggregates posts back to creators.
- Weak or missing global discover does not get misreported as a success.
- `account-first` profile lookup produces enough profile evidence for L2.
- Shared downstream L2/L3/S2 contract remains compatible.
