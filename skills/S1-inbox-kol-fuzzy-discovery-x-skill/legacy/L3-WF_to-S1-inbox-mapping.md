---
description: "[Legacy Workflow] L3 到 S1 Inbox 映射工作流 | 已降级为历史中间产物说明。"
---

# Legacy: L3-WF_to-S1-inbox-mapping

> 这份文件是历史保留版。  
> 当前主业务链路已从 `L3 -> S1` 转向 `L3 -> S2`。  
> `S1` 仍可作为中间产物或兼容产物使用，但不再是默认交付。

## 现在如何使用

- 如果你仍需要传统 `S1 Inbox CSV`，参考 `WF/legacy/L3-WF_to-S1-inbox-mapping-full.md`
- 如果你当前目标是冷启动触达，请优先使用 `WF/L3-WF_to-S2-cold-outreach-mapping.md`

## Legacy 定位

- `S1` 适合做 shortlist review / 入池
- `S2` 适合直接进入 cold outreach 与 Gmail draft 流程
- 因此，`L3 -> S1` 不再作为默认主链路

---
📍 Legacy note only. Keep for backward compatibility.
