# YouTube KOL Fuzzy Discovery

用于 YouTube 达人的 L1 发现、L2 补全、L3 shortlist 和 S2 导出。若启用 YouTube API 或 Gmail 预填，仅能使用接收方自己的 API 与 OAuth 配置。

V2 依赖：`base,social`；浏览器加 `browser`，Gmail 预填加 `gmail`。模块已纳入可安装元数据，未做真实账号验收。
